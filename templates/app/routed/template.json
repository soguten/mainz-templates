{
    "kind": "app",
    "name": "routed",
    "target": {
        "name": "{{appName}}",
        "rootDir": "{{rootDir}}",
        "appFile": "{{rootDir}}/src/app.ts",
        "appId": "{{appId}}",
        "outDir": "{{outDir}}"
    }
}
